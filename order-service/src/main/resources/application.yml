eureka:
  client:
    serviceUrl:      # eureka server的路径
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true # 使用ip注册
spring:
  application:
    name: order-service
server:
  port: 8080